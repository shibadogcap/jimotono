# DESIGN.MD

## 1. メモリ帯域とレイテンシの計算

### 1.1 DDR5実測帯域

| 構成 | 実測帯域 | 備考 |
|---|---|---|
| 2×48GB DDR5-6400 | 約97.7 GB/s | デュアルチャネル |
| 4×48GB DDR5-5200 | 約79.8 GB/s | クアッドチャネル |
| LPCAMM2 (LPDDR5X 7500) | 理論120.0 GB/s | DDR5-5600比34%増 |

### 1.2 アクティブパラメータの逆算

目標：シングルコアで5 tok/s（N100クラス）
- 利用可能帯域：80 × 0.7 = 56 GB/s
- 1トークンあたり：56 ÷ 5 = 11.2 GB/token
- INT4（0.5 byte/param）での上限：22B
- 実効値（1/4〜1/2）：5.5B〜11B

結論：A100Mは極めて保守的で安全な値。

## 2. シングルコア vs マルチコア

### 2.1 マルチコアの「地獄」

- llama.cppのNUMA関連PR：CPU使用率の約50%がスレッド同期に費やされる。2ソケットから1ソケットに減らすとggml_barrier呼び出しが約40%減少
- クロスNUMAメモリアクセスは大幅な性能低下

### 2.2 1C1Tの根拠

- Curieが17Bパラメータを単一CPUコアで33 tok/s
  https://github.com/Scottcjn/colibri
- シングルスレッドなら帯域競合時も予測可能な劣化に留まる
- マルチスレッド化による同期オーバーヘッド（最大50%）を回避

### 2.3 性能劣化要因

- マルチスレッド化による同期オーバーヘッド（最大50%）
- ブラウザエディタとのメモリ帯域競合
- AVX-512のダウンクロック（最大30%）
- NUMAノード間のメモリアクセス

## 3. SSDストリーミング設計

### 3.1 O_DIRECT vs ページキャッシュ

| 方式 | 利点 | 欠点 |
|---|---|---|
| O_DIRECT | 予測可能なレイテンシ、キャッシュ汚染なし | ウォームパスでページキャッシュより遅い場合がある |
| ページキャッシュ | 十分なRAMがあれば高速 | 予測不能なレイテンシ、メモリ圧迫 |

推奨：Colibri方式のLRUキャッシュ + OSページキャッシュの階層。O_DIRECTはコールドエキスパートのみに限定。

### 3.2 io_uring + O_DIRECT

- NeuroPrefetcher：io_uring + O_DIRECTで活性ニューロンの15〜18%のみを読み込み
  https://github.com/nobeldhar/NeuroPrefetcher
- llama.cpp比で7.9〜12.0倍高速（Jetson AGX Orin、14GiBキャップ下）
- major page fault：11,453/s → 0.21/s、CPU iowait：77% → 4%

### 3.3 性能劣化要因

- SSDのランダムアクセスレイテンシ（20〜100μs）はCPUにとって無限に近い
- ブロッキングI/Oは絶対に許容されない
- ページキャッシュの予測不能なエビクション

## 4. カーネル最適化

### 4.1 計算順序

- トークンの並べ替え：ルーティング後、トークンをエキスパートIDでソートし、エキスパートごとにGEMMを打つ（Megablocks方式）
- Mが可変だとカーネルが崩れる：capacity factorで上限を切り、超過分はdropかpad
- 逆伝播の順序：dXを先に計算して伝播させ、dWは後回し。dW計算→更新→書き戻しをオーバーラップ

### 4.2 分岐の除去

- CMOV / マスク / predication：分岐を条件実行に変換
- テンプレート特殊化：Cマクロ、Zig comptimeでコンパイル時に分岐を消す
- ジャンプテーブル：エキスパートディスパッチ用

### 4.3 レイテンシ隠蔽

- FMAレイテンシは4〜5サイクル。独立したFMAチェーンが4本以上あれば隠れる
- リダクション（sum, max）を分割する（4本の部分和を並行に）
- softmax中に次の層の重みprefetch、KVキャッシュへの非テンポラル書き込み

### 4.4 性能劣化要因

- 内側ループのデータ依存分岐（分岐ミスペナルティ15〜20サイクル）
- OoOウィンドウを依存チェーンで埋めるとストール
- 除算（20サイクル近い）をそのまま使う

## 5. モデル形式＝実行計画

### 5.1 エクスポート時の最適化

1. 共起クラスタリング：一緒に選ばれるエキスパートをディスク上で隣接配置
2. プリトランスポーズ＆プリブロック：カーネルが読む順に64×64タイルで並べ替え
3. ゲート/アップ/ダウンのインターリーブ：同一ページに配置
4. ホット/コールド分離：頻出エキスパートをファイル先頭に
5. LUTとスケールを重みと同じページに
6. レイヤごとのカーネルディスパッチテーブル
7. RoPEテーブル、マスク、アテンションバイアスを事前計算

### 5.2 性能劣化要因

- エキスパートがディスク上でランダム配置されていると、ランダム4KB読みが頻発
- メタデータ読みが別ページにあると、余分なI/Oが発生

## 6. 目標数値

| 指標 | 目標値 | 実測 (N150) | 根拠 |
|---|---|---|---|
| カーネル呼び出し回数 | 50回/token以下 | — (未計測) | DeepSeek V4の673 tok/s事例では2000回→少数回に削減 |
| 1トークンあたりのNVMe読み込み | 103 MiB以下 | — (未計測) | NeuroPrefetcherの実測値 |
| ルーティング予測精度 | 99%以上 | — (未計測) | NeuroPrefetcherのcentroid選択精度 |
| スイッチ率削減 | 59%以上 | — (未計測) | StickyMoEの実測値 |
| キャッシュミス削減 | 3.92倍以上 | — (未計測) | StickyMoEの実測値 |
| FMAレイテンシ隠蔽のための独立チェーン数 | 4本以上 | — (未計測) | FMAレイテンシ4〜5サイクル、スループット1〜2/サイクル |
| 分岐ミスペナルティの許容回数 | 1トークンあたり100回以下 | — (未計測) | 15〜20サイクル × 100 = 1500〜2000サイクル（予算の1%以下） |
| 1誤差伝播あたりのクロック数 | 20Mサイクル以下 | — (未計測、step実測で代替) | 100M FLOP → 50M FMA → 4-wide SIMD × 2 FMA/cycle で約6.25M。逆伝播2x、オーバーヘッド3xで約20M |
| 1トークン順伝播のクロック数 | 6Mサイクル以下 | 98k cycles (OK) | 50M FMA ÷ (4-wide × 2/cycle) = 6.25M |
| 1トークン逆伝播のクロック数 | 12Mサイクル以下 | 144k cycles (OK) | 順伝播の約2倍 |
| 学習1ステップのクロック数 | 20Mサイクル以下 | 2.24M cycles (OK) | 順伝播6M + 逆伝播12M + 更新2M |
| softmaxのレイテンシ | 20サイクル以下/行 | — (未計測) | exp2 + 逆数近似 + NR 1回 |
| RMSNormのレイテンシ | 10サイクル以下/行 | — (未計測) | 部分和4本並行 |

実測の前提：Intel N150上のcycle benchによる小次元参照値 (fwd_sum 98403 / bwd_sum 143510 /
step_est 2241913 cycles。周波数は既定3.2GHz仮定のwall-to-cycles換算のためupper-bound的参照)。
A100M級への外挿はTODO。詳細は `results/bench/20260913-n150.md` を参照。