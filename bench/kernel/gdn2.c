// bench: GDN-2 kernel microbench スケルトン (Phase 1で実装)。
// ROADMAP 5.2形式: harness=cases=metricを固定し、Phase 1で中身を埋める。
// cases: seq_len {512, 2048, 8192}, batch {1, 8}; metric: us/call.
#include <stdio.h>

int main(void) {
    printf("gdn2 bench: STUB (Phase 1で実装)\n");
    printf("cases: seq_len={512,2048,8192} batch={1,8} metric=us/call\n");
    return 0;
}
