// bench: T-MAC LUT kernel microbench スケルトン (Phase 1で実装)。
// cases: bit-width {1,2,4} x act {int8}; metric: us/call.
#include <stdio.h>

int main(void) {
    printf("tmac_lut bench: STUB (Phase 1で実装)\n");
    printf("cases: bits={1,2,4} act=int8 metric=us/call\n");
    return 0;
}
